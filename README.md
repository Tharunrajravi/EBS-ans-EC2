project: EC2 and EBS Hands-On Task

description: 
  - Launched two EC2 instances (Linux and Windows) with web servers configured during instance creation.
  - Created a 5 GB EBS volume and attached it to a Linux EC2 instance.
  - Took a snapshot of the EBS volume and created a new volume from the snapshot.
  - Attached the snapshot-based EBS volume to the Windows EC2 instance.
